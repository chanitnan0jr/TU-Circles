# API Samples Placeholder

This directory holds raw, redacted JSON response samples from the official TU REST API (captured via Postman).

- `student-profile.json` (Pending real Postman capture commit)
- `employee-profile.json` (Pending real Postman capture commit)

**Note:** Do not fabricate unconfirmed fields in sample files. Replace placeholder notes once real API responses are captured.
